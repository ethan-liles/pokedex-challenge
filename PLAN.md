# Take home portion plan

TODO

1. Create boiler plate skeleton for React + Typescript + Redux RTK Query + Bun single page app.
2. See if can pull in pokedex api spec and use RTK query codegen to generate RTK hooks.
3. Display the original 151 Pokémon with their name, sprite, and type(s). Include a way to filter by type or by name.
4. README instructions for setup